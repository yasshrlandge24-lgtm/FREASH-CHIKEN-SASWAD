'use client';import {useEffect,useState} from 'react';import {Plus,Trash2} from 'lucide-react';
export default function Categories(){const[data,setData]=useState<any[]>([]);const[form,setForm]=useState({name:'',slug:'',description:'',displayOrder:0});const[open,setOpen]=useState(false);async function load(){const r=await fetch('/api/admin/categories');if(r.ok)setData((await r.json()).data)}useEffect(()=>{load()},[]);async function save(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/admin/categories',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...form,displayOrder:Number(form.displayOrder)})});if(r.ok){setForm({name:'',slug:'',description:'',displayOrder:0});setOpen(false);load()}}async function remove(id:string){await fetch('/api/admin/categories/'+id,{method:'DELETE'});load()}return <main className="dashboard"><div className="shop-top"><div><span className="eyebrow">Catalog</span><h1>Categories</h1><p>Organise the fresh counter.</p></div><button className="admin-btn" onClick={()=>setOpen(!open)}><Plus size={14}/> New category</button></div>{open&&<form onSubmit={save} className="admin-card admin-form" style={{marginBottom:18}}><input className="field" required placeholder="Name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input className="field" required placeholder="Slug" value={form.slug} onChange={e=>setForm({...form,slug:e.target.value})}/><input className="field full" placeholder="Description" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/><input className="field" type="number" placeholder="Display order" value={form.displayOrder} onChange={e=>setForm({...form,displayOrder:Number(e.target.value)})}/><button className="admin-btn">Create category</button></form>}<div className="admin-card"><table className="admin-table"><thead><tr><th>Name</th><th>Slug</th><th>Order</th><th>Status</th><th/></tr></thead><tbody>{data.map(x=><tr key={x.id}><td><b>{x.name}</b></td><td>{x.slug}</td><td>{x.displayOrder}</td><td>{x.active?'Active':'Hidden'}</td><td><button onClick={()=>remove(x.id)} className="btn" style={{padding:7,color:'var(--red)'}}><Trash2 size={13}/></button></td></tr>)}</tbody></table></div></main>}
