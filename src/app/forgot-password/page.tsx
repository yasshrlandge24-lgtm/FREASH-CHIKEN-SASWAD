'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
export default function ForgotPasswordPage(){
 const router=useRouter(); const [email,setEmail]=useState(''),[message,setMessage]=useState(''),[resetUrl,setResetUrl]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);
 async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');setMessage('');setResetUrl('');try{const r=await fetch('/api/auth/forgot-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const b=await r.json();if(!r.ok){setError(b.error||'Could not create reset link.');return;}setMessage(b.message);if(b.developmentResetUrl)setResetUrl(b.developmentResetUrl);}catch{setError('Something went wrong. Please try again.')}finally{setLoading(false)}}
 return <main className="auth-page"><div className="auth-card"><span className="eyebrow">Freash Chiken · Account recovery</span><h1>Forgot password?</h1><p className="auth-sub">Enter the email linked to your account. We will create a secure reset link.</p><form onSubmit={submit} className="auth-form"><label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" autoComplete="email" required/></label>{error&&<p className="auth-error">{error}</p>}{message&&<p className="auth-notice">{message}</p>}{resetUrl&&<a className="auth-reset-link" href={resetUrl}>Open reset link (development)</a>}<button className="btn btn-dark auth-submit" disabled={loading}>{loading?'Generating…':'Send reset link'}</button></form><p className="auth-footer"><button type="button" className="auth-link-button inline" onClick={()=>router.push('/login')}>Back to sign in</button></p></div></main>
}
